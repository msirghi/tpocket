mutation AddExpense($categoryId: Float!, $amount: Float!) {
  addExpense(categoryId: $categoryId, amount: $amount) {
    category {
      id
      name
    }
    expense {
      id
      amount
    }
  }
}